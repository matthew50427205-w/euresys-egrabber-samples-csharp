<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net6.0</TargetFramework>
    <RootNamespace>05CameraParameters</RootNamespace>
    <AssemblyName>05_CameraParameters</AssemblyName>
  </PropertyGroup>

  <!-- 공통 설정(EGrabberDir, ConfigDir, EGrabber.NET 참조, Coaxlink_NetApi.dll/config-*.js 복사)은
       솔루션 루트의 Directory.Build.props 에서 자동 적용됩니다. -->

</Project>
